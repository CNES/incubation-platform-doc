[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "centre_incub"
authors = [{name = "Thomas Roux", email = "thomas.roux@cnes.fr"}]
dynamic = ["version", "description"]
